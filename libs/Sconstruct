Library(target = 'base',
        source = ['thread.cc',
                  'logging.cc',
                  'skiplist.cc',
                  'consist_hash.cc'
                 ],
        LIBS = ['pthread'],
        )
Library(target = 'trie_tree',
        source = ['trie_tree.cc'])
